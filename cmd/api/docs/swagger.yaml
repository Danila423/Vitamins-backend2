definitions:
  auth.AuthRequest:
    properties:
      email:
        type: string
      password:
        type: string
    type: object
  auth.ErrorResponse:
    properties:
      code:
        type: string
      message:
        type: string
    type: object
  auth.PasswordChangeConfirmRequest:
    properties:
      changeToken:
        type: string
      password:
        type: string
      passwordConfirm:
        type: string
    type: object
  auth.PasswordChangeVerifyRequest:
    properties:
      code:
        type: string
    type: object
  auth.PasswordChangeVerifyResponse:
    properties:
      changeToken:
        type: string
    type: object
  auth.PasswordResetConfirmRequest:
    properties:
      password:
        type: string
      passwordConfirm:
        type: string
      resetToken:
        type: string
    type: object
  auth.PasswordResetRequest:
    properties:
      email:
        type: string
    type: object
  auth.PasswordResetVerifyRequest:
    properties:
      code:
        type: string
      email:
        type: string
    type: object
  auth.PasswordResetVerifyResponse:
    properties:
      resetToken:
        type: string
    type: object
  auth.TokenPair:
    properties:
      accessToken:
        type: string
      refreshToken:
        type: string
    type: object
  auth.UpdateProfileRequest:
    properties:
      email:
        type: string
      firstName:
        type: string
      lastName:
        type: string
    type: object
  auth.UserProfileResponse:
    properties:
      email:
        type: string
      firstName:
        type: string
      id:
        type: integer
      lastName:
        type: string
    type: object
info:
  contact: {}
  description: API для регистрации и авторизации
  title: Vitamins API
  version: "1.0"
paths:
  /auth/login:
    post:
      consumes:
      - application/json
      description: Проверяет учетные данные и возвращает новую пару токенов
      parameters:
      - description: Данные пользователя
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/auth.AuthRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.TokenPair'
        "400":
          description: EMAIL_AND_PASSWORD_REQUIRED, EMAIL_REQUIRED, INVALID_EMAIL_FORMAT,
            PASSWORD_REQUIRED, INVALID_PASSWORD_FORMAT
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "401":
          description: INVALID_CREDENTIALS
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
      summary: Авторизация пользователя
      tags:
      - auth
  /auth/password/reset/confirm:
    post:
      consumes:
      - application/json
      description: Устанавливает новый пароль по resetToken
      parameters:
      - description: Reset token и новый пароль
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/auth.PasswordResetConfirmRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: PASSWORD_REQUIRED, INVALID_PASSWORD_FORMAT, PASSWORD_CONFIRMATION_MISMATCH,
            RESET_TOKEN_REQUIRED
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "401":
          description: RESET_SESSION_INVALID, RESET_SESSION_EXPIRED
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
      summary: Смена пароля
      tags:
      - auth
  /auth/password/reset/request:
    post:
      consumes:
      - application/json
      description: Отправляет код на e-mail через SMTP
      parameters:
      - description: E-mail
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/auth.PasswordResetRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: EMAIL_REQUIRED, INVALID_EMAIL_FORMAT
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "429":
          description: TOO_MANY_REQUESTS
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
      summary: Запрос кода восстановления пароля
      tags:
      - auth
  /auth/password/reset/verify:
    post:
      consumes:
      - application/json
      description: Проверяет код и возвращает resetToken
      parameters:
      - description: E-mail и код
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/auth.PasswordResetVerifyRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.PasswordResetVerifyResponse'
        "400":
          description: EMAIL_REQUIRED, INVALID_EMAIL_FORMAT, RESET_CODE_REQUIRED
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "401":
          description: RESET_CODE_INVALID, RESET_CODE_EXPIRED, RESET_CODE_TOO_MANY_ATTEMPTS
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
      summary: Проверка кода восстановления
      tags:
      - auth
  /auth/register:
    post:
      consumes:
      - application/json
      description: Создает учетную запись и возвращает пару access/refresh токенов
      parameters:
      - description: Данные пользователя
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/auth.AuthRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.TokenPair'
        "400":
          description: EMAIL_AND_PASSWORD_REQUIRED, EMAIL_REQUIRED, INVALID_EMAIL_FORMAT,
            PASSWORD_REQUIRED, INVALID_PASSWORD_FORMAT
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "409":
          description: EMAIL_ALREADY_EXISTS
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
      summary: Регистрация пользователя
      tags:
      - auth
  /users/me:
    patch:
      consumes:
      - application/json
      description: Обновляет имя, фамилию и/или e-mail текущего пользователя
      parameters:
      - description: Данные профиля
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/auth.UpdateProfileRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.UserProfileResponse'
        "400":
          description: NO_FIELDS_TO_UPDATE, EMAIL_REQUIRED, INVALID_EMAIL_FORMAT
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "401":
          description: AUTH_REQUIRED, INVALID_TOKEN
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "404":
          description: USER_NOT_FOUND
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "409":
          description: EMAIL_ALREADY_EXISTS
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Обновление профиля
      tags:
      - users
  /users/me/password/change/confirm:
    post:
      consumes:
      - application/json
      description: Устанавливает новый пароль по changeToken
      parameters:
      - description: Change token и новый пароль
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/auth.PasswordChangeConfirmRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: PASSWORD_REQUIRED, INVALID_PASSWORD_FORMAT, PASSWORD_CONFIRMATION_MISMATCH,
            CHANGE_TOKEN_REQUIRED
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "401":
          description: CHANGE_SESSION_INVALID, CHANGE_SESSION_EXPIRED
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "500":
          description: REDIS_NOT_CONFIGURED
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Смена пароля
      tags:
      - users
  /users/me/password/change/request:
    post:
      consumes:
      - application/json
      description: Отправляет код на e-mail текущего пользователя
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "401":
          description: AUTH_REQUIRED, INVALID_TOKEN
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "404":
          description: USER_NOT_FOUND
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "429":
          description: TOO_MANY_REQUESTS
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "500":
          description: MAILER_NOT_CONFIGURED, REDIS_NOT_CONFIGURED
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Запрос кода смены пароля
      tags:
      - users
  /users/me/password/change/verify:
    post:
      consumes:
      - application/json
      description: Проверяет код и возвращает changeToken
      parameters:
      - description: Код из письма
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/auth.PasswordChangeVerifyRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.PasswordChangeVerifyResponse'
        "400":
          description: CHANGE_CODE_REQUIRED
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "401":
          description: CHANGE_CODE_INVALID, CHANGE_CODE_TOO_MANY_ATTEMPTS, AUTH_REQUIRED,
            INVALID_TOKEN
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "404":
          description: USER_NOT_FOUND
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
        "500":
          description: REDIS_NOT_CONFIGURED
          schema:
            $ref: '#/definitions/auth.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Проверка кода смены пароля
      tags:
      - users
securityDefinitions:
  BearerAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
