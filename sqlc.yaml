version: "2"
sql:
  - schema: "pkg/db/models.sql"
    queries: "pkg/db/queries.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "pkg/db"
        sql_package: "pgx/v5"
